{
  "name": "portfolio-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "pages:dev": "npm run build && WRANGLER_LOG_PATH=.wrangler/logs wrangler pages dev dist",
    "deploy": "npm run build && WRANGLER_LOG_PATH=.wrangler/logs wrangler pages deploy dist --project-name whiteyedcat --branch master",
    "deploy:preview": "npm run build && WRANGLER_LOG_PATH=.wrangler/logs wrangler pages deploy dist --project-name whiteyedcat",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.2.1",
    "masonry-layout": "^4.2.2",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/masonry-layout": "^4.2.8",
    "@types/qrcode": "^1.5.6",
    "wrangler": "^4.87.0"
  }
}
